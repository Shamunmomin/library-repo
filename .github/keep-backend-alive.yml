name: Keep Backend Alive

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  keep-alive:
    runs-on: ubuntu-latest

    steps:
      - name: Call backend warmup
        run: |
          curl -f https://library-repo-n2eu.onrender.com/api/health/warmup